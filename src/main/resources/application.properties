spring.application.name=CommercePJT

# H2 Database
spring.datasource.url=jdbc:h2:mem:commercedb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true

# JPA
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 세션 유효 시간: 마지막 요청 이후 24시간
server.servlet.session.timeout=24h
# JavaScript에서 세션 쿠키 접근 방지
server.servlet.session.cookie.http-only=true
# HTTPS 환경에서만 쿠키 전송
# 로컬 HTTP 테스트에서는 false, 배포 환경에서는 true 권장
server.servlet.session.cookie.secure=false
# 다른 사이트에서 쿠키를 보내는 범위 제한
server.servlet.session.cookie.same-site=lax

# 테스트전용데이터
spring.jpa.defer-datasource-initialization=true

# 페이징 기본값 (과제: 페이지 번호 1부터 시작, 페이지당 10개)
spring.data.web.pageable.one-indexed-parameters=true
spring.data.web.pageable.default-page-size=10

# 페이징 기본값 (과제: 페이지 번호 1부터 시작, 페이지당 10개)
spring.data.web.pageable.one-indexed-parameters=true
spring.data.web.pageable.default-page-size=10